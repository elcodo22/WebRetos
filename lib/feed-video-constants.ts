/** Segundo preferido al extraer miniatura (se prueban más si el frame sale vacío). */
export const FEED_THUMB_FRAME_SECONDS = 2;

/** Relación ancho/alto de las miniaturas del feed (16:9). */
export const FEED_THUMB_ASPECT = 16 / 9;
